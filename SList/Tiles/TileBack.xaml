<UserControl x:Class="ProTile.TileBack"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	FontFamily="{StaticResource PhoneFontFamilySemiBold}"
    FontSize="{StaticResource PhoneFontSizeLarge}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    Width="173" Height="173">
	<Grid x:Name="LayoutRoot" Width="173" Height="173">
		<Image x:Name="bg" Source="/mango.png"/>
		<TextBlock x:Name="text" Margin="8,0" TextWrapping="Wrap" VerticalAlignment="Top" Text="MESSAGE"/>
		<TextBlock x:Name="number" Margin="8,0,8,4" TextWrapping="Wrap" VerticalAlignment="Bottom" Text="NUMBER"/>
	</Grid>
</UserControl>
